{
  "note": "Deliberate differences between the OpenCodex provider registry snapshot and Quotio's provider catalog. An omission is a reference provider Quotio deliberately does not expose. Every entry is enforced: an undeclared difference fails provider-catalog.test.ts, and a declared difference that no longer exists fails it too. An omitted provider must be absent from the catalog and from the parity manifest.",
  "deviations": [
    {
      "id": "kiro",
      "field": "authKind",
      "reference": "oauth",
      "current": "key",
      "reason": "Quotio onboards Kiro by importing an existing credential (crates/gateway/src/management/creds.rs) instead of driving a browser OAuth round trip. The reference flow (opencodex/src/oauth/kiro.ts) also reads a local credential file, so the imported shape is identical; only the entry point differs."
    },
    {
      "id": "command-code",
      "field": "label",
      "reference": "Command Code - Auth",
      "current": "Command Code",
      "reason": "The reference label distinguishes two onboarding modes in its own picker. Quotio exposes a single Command Code entry whose OAuth callback lifecycle is implemented, so the mode suffix would be misleading in this UI."
    }
  ],
  "omissions": []
}
